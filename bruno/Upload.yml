info:
  name: Upload
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8085/upload
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
